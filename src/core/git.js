class GitUserInfo {
	constructor() {
		this.username = ''
		this.token = ''
		this.gist_token = ''
	}
}

class Git {
	/**
	 * Authenticate the user to github.
	 * @param {*GitUserInfo} GitInfo Information about the user.
	 */
	Authenticate(info) {

	}


}